"""Platxa Agent Generator Scripts Package.

This package contains all the generation, validation, and utility modules
for the Platxa Agent Generator system.

Submodules are imported lazily — use ``from platxa_agent_generator.<module>
import X`` or ``import platxa_agent_generator.<module>`` to load a specific
module. This avoids pulling in every submodule (and its transitive
dependencies such as ``yaml``) when only one is needed.
"""

from __future__ import annotations

__version__ = "1.1.0"
__author__ = "Platxa"
